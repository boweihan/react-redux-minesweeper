
const INITIAL_CONTROLS_STATE = {
	numRows: 16,
	numCols: 16,
	numMines: 40
};


export default function controls (state = INITIAL_CONTROLS_STATE, action) {
	switch (action.type) {
		default:
			return state;
	}
}